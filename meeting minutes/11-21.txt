Members Present: Joel Barna

Changes:

*Changed shake to be easier to shake the device and fire
*Removed rest client variables in playGame
*moved factory creation outside of loop
* Changed textAdapter to gridAdapter
* Abstracted gridAdapter array to just be mapItems,
    then gridAdapter just uses the gui factory to get the view
    to display
